.row
  - flash.each do |name, msg|
    = content_tag :div, msg, class: name
    
  %h2= @post.title
  %p
    %em= @post.category_title
  %p= @post.body

  = link_to 'Edit', edit_post_path(@post)
  \|
  = link_to 'Back', posts_path
